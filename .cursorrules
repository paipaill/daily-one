# Daily-one 核心维护与重构要求

1. **高效执行**：不要随意推翻原有设计和架构，改动应保持克制。遵循 "If it ain't broke, don't fix it" 原则。
2. **坚守原代码约束**：完全依赖原项目的视觉方案（极简、暗调、Vanilla JS/CSS），不允许私自引入外部库或者框架。重构时必须最大程度复用原项目代码，仅做必要的物理拆分以优化文件结构。
3. **节省 Token 开销**：代码搬移时，除了直接命中痛点的“内存泄漏”或“存储越界”逻辑，其余代码请原样复制，不要做无谓的代码清理、重命名或重编排，以最短的补丁完成最大的优化。
4. **性能和安全红线**：始终避免会导致内存激增的设计（如无脑将大体积图片全部序列化存留于全局对象），以及随时考虑 LocalStorage 写入上限的容错降级。
